import { Router } from 'express'
const router = Router()

router.use('/workspace-slug-check/')
router.use('/')
router.use('/<str:slug>/')
router.use('/<str:slug>/invite/')
router.use('/<str:slug>/invitations/')
router.use('/<str:slug>/invitations/<uuid:pk>/')
router.use('/<str:slug>/members/')
router.use('/<str:slug>/members/<uuid:pk>/')
router.use('/<str:slug>/teams/')
router.use('/<str:slug>/teams/<uuid:pk>/')
router.use('/<str:slug>/workspace-members/me/')
router.use('/<str:slug>/workspace-views/')
router.use('/<str:slug>/workspace-themes/')
router.use('/<str:slug>/workspace-themes/<uuid:pk>/')
router.use('/<str:slug>/projects/')
router.use('/<str:slug>/projects/<uuid:pk>/')
router.use('/<str:slug>/project-identifiers/')
router.use('/<str:slug>/projects/<uuid:project_id>/invite/')
router.use('/<str:slug>/projects/<uuid:project_id>/members/')
router.use('/<str:slug>/projects/<uuid:project_id>/members/<uuid:pk>/')
router.use('/<str:slug>/projects/<uuid:project_id>/members/<uuid:pk>/')
router.use('/<str:slug>/projects/<uuid:project_id>/members/add/')
router.use('/<str:slug>/projects/join/')
router.use('/<str:slug>/projects/<uuid:project_id>/team-invite/')
router.use('/<str:slug>/projects/<uuid:project_id>/invitations/')
router.use('/<str:slug>/projects/<uuid:project_id>/invitations/<uuid:pk>/')
router.use('/<str:slug>/projects/<uuid:project_id>/project-views/')
router.use('/<str:slug>/projects/<uuid:project_id>/project-members/me/')
router.use('/<str:slug>/user-favorite-projects/')
router.use('/<str:slug>/user-favorite-projects/<uuid:project_id>/')
router.use('/<str:slug>/projects/<uuid:project_id>/states/')
router.use('/<str:slug>/projects/<uuid:project_id>/states/<uuid:pk>/')
router.use('/<str:slug>/projects/<uuid:project_id>/project-estimates/')
router.use('/<str:slug>/projects/<uuid:project_id>/estimates/')
router.use('/<str:slug>/projects/<uuid:project_id>/estimates/<uuid:estimate_id>/')
router.use('/<str:slug>/projects/<uuid:project_id>/views/')
router.use("/<str:slug>/projects/<uuid:project_id>/views/<uuid:pk>/")
router.use("/<str:slug>/projects/<uuid:project_id>/views/<uuid:view_id>/issues/")
router.use("/<str:slug>/projects/<uuid:project_id>/user-favorite-views/")
router.use("/<str:slug>/projects/<uuid:project_id>/user-favorite-views/<uuid:view_id>/")
router.use("/<str:slug>/projects/<uuid:project_id>/cycles/")
router.use("/<str:slug>/projects/<uuid:project_id>/cycles/<uuid:pk>/")
router.use("/<str:slug>/projects/<uuid:project_id>/cycles/<uuid:cycle_id>/cycle-issues/")
router.use("/<str:slug>/projects/<uuid:project_id>/cycles/<uuid:cycle_id>/cycle-issues/<uuid:pk>/")
router.use("/<str:slug>/projects/<uuid:project_id>/cycles/date-check/")
router.use("/<str:slug>/projects/<uuid:project_id>/user-favorite-cycles/")
router.use("/<str:slug>/projects/<uuid:project_id>/user-favorite-cycles/<uuid:cycle_id>/")
router.use("/<str:slug>/projects/<uuid:project_id>/cycles/<uuid:cycle_id>/transfer-issues/")
router.use("/<str:slug>/projects/<uuid:project_id>/issues/")
router.use("/<str:slug>/projects/<uuid:project_id>/issues/<uuid:pk>/")
router.use("/<str:slug>/issues/")
router.use("/<str:slug>/projects/<uuid:project_id>/issue-labels/")
router.use("/<str:slug>/projects/<uuid:project_id>/issue-labels/<uuid:pk>/")
router.use("/<str:slug>/projects/<uuid:project_id>/bulk-create-labels/")
router.use("/<str:slug>/projects/<uuid:project_id>/bulk-delete-issues/")
router.use("/<str:slug>/projects/<uuid:project_id>/bulk-import-issues/<str:service>/")
router.use("/<str:slug>/my-issues/")
router.use("/<str:slug>/projects/<uuid:project_id>/issues/<uuid:issue_id>/sub-issues/")
router.use("/<str:slug>/projects/<uuid:project_id>/issues/<uuid:issue_id>/issue-links/")
router.use("/<str:slug>/projects/<uuid:project_id>/issues/<uuid:issue_id>/issue-links/<uuid:pk>/")
router.use("/<str:slug>/projects/<uuid:project_id>/issues/<uuid:issue_id>/issue-attachments/")
router.use("/<str:slug>/projects/<uuid:project_id>/issues/<uuid:issue_id>/issue-attachments/<uuid:pk>/")
router.use("/<str:slug>/projects/<uuid:project_id>/issues/<uuid:issue_id>/history/")
router.use("/<str:slug>/projects/<uuid:project_id>/issues/<uuid:issue_id>/comments/")
router.use("/<str:slug>/projects/<uuid:project_id>/issues/<uuid:issue_id>/comments/<uuid:pk>/")
router.use("/<str:slug>/projects/<uuid:project_id>/issue-properties/")
router.use("/<str:slug>/projects/<uuid:project_id>/issue-properties/<uuid:pk>/")
router.use("/<str:slug>/file-assets/")
router.use("/file-assets/<uuid:workspace_id>/<str:asset_key>/")
router.use("/<str:slug>/projects/<uuid:project_id>/modules/")
router.use("/<str:slug>/projects/<uuid:project_id>/modules/<uuid:pk>/")
router.use("/<str:slug>/projects/<uuid:project_id>/modules/<uuid:module_id>/module-issues/")
router.use("/<str:slug>/projects/<uuid:project_id>/modules/<uuid:module_id>/module-issues/<uuid:pk>/")
router.use("/<str:slug>/projects/<uuid:project_id>/modules/<uuid:module_id>/module-links/")
router.use("/<str:slug>/projects/<uuid:project_id>/modules/<uuid:module_id>/module-links/<uuid:pk>/")
router.use("/<str:slug>/projects/<uuid:project_id>/user-favorite-modules/")
router.use("/<str:slug>/projects/<uuid:project_id>/user-favorite-modules/<uuid:module_id>/")
router.use("/<str:slug>/projects/<uuid:project_id>/bulk-import-modules/<str:service>/")
router.use("/<str:slug>/projects/<uuid:project_id>/pages/")
router.use("/<str:slug>/projects/<uuid:project_id>/pages/<uuid:pk>/")
router.use("/<str:slug>/projects/<uuid:project_id>/pages/<uuid:page_id>/page-bloks/")
router.use("/<str:slug>/projects/<uuid:project_id>/pages/<uuid:page_id>/page-blocks/<uuid:pk>/")
router.use("/<str:slug>/projects/<uuid:project_id>/user-favorite-pages/")
router.use("/<str:slug>/projects/<uuid:project_id>/user-favorite-pages/<uuid:page_id>/")
router.use("/<str:slug>/projects/<uuid:project_id>/pages/<uuid:page_id>/page-blocks/<uuid:page_block_id>/issues/")
router.use("/<str:slug>/workspace-integrations/")
router.use("/<str:slug>/workspace-integrations/<str:provider>/")
router.use("/<str:slug>/workspace-integrations/<uuid:pk>/provider/")
router.use("/<str:slug>/workspace-integrations/<uuid:workspace_integration_id>/github-repositories/")
router.use("/<str:slug>/projects/<uuid:project_id>/workspace-integrations/<uuid:workspace_integration_id>/github-repository-sync/")
router.use("/<str:slug>/projects/<uuid:project_id>/workspace-integrations/<uuid:workspace_integration_id>/github-repository-sync/<uuid:pk>/")
router.use("/<str:slug>/projects/<uuid:project_id>/github-repository-sync/<uuid:repo_sync_id>/github-issue-sync/")
router.use("/<str:slug>/projects/<uuid:project_id>/github-repository-sync/<uuid:repo_sync_id>/bulk-create-github-issue-sync/")
router.use("/<str:slug>/projects/<uuid:project_id>/github-repository-sync/<uuid:repo_sync_id>/github-issue-sync/<uuid:pk>/")
router.use("/<str:slug>/projects/<uuid:project_id>/github-repository-sync/<uuid:repo_sync_id>/github-issue-sync/<uuid:issue_sync_id>/github-comment-sync/")
router.use("/<str:slug>/projects/<uuid:project_id>/github-repository-sync/<uuid:repo_sync_id>/github-issue-sync/<uuid:issue_sync_id>/github-comment-sync/<uuid:pk>/")
router.use("/<str:slug>/projects/<uuid:project_id>/workspace-integrations/<uuid:workspace_integration_id>/project-slack-sync/")
router.use("/<str:slug>/projects/<uuid:project_id>/workspace-integrations/<uuid:workspace_integration_id>/project-slack-sync/<uuid:pk>/")
router.use("/<str:slug>/importers/<str:service>/")
router.use("/<str:slug>/projects/importers/<str:service>/")
router.use("/<str:slug>/importers/")
router.use("/<str:slug>/importers/<str:service>/<uuid:pk>/")
router.use("/<str:slug>/projects/<uuid:project_id>/service/<str:service>/importers/<uuid:importer_id>/")
router.use("/<str:slug>/projects/<uuid:project_id>/search/")
router.use("/<str:slug>/projects/<uuid:project_id>/search-issues/")
router.use("/<str:slug>/projects/<uuid:project_id>/ai-assistant/")
router.use("/<str:slug>/projects/<uuid:project_id>/inboxes/")
router.use("/<str:slug>/projects/<uuid:project_id>/inboxes/<uuid:pk>/")
router.use("/<str:slug>/projects/<uuid:project_id>/inboxes/<uuid:inbox_id>/inbox-issues/")
router.use("/<str:slug>/projects/<uuid:project_id>/inboxes/<uuid:inbox_id>/inbox-issues/<uuid:pk>/")
router.use("/<str:slug>/analytics/")
router.use("/<str:slug>/analytic-view/")
router.use("/<str:slug>/analytic-view/<uuid:pk>/")
router.use("/<str:slug>/export-analytics/")
router.use("/<str:slug>/default-analytics/")

export default router